

@using AcodexWebSite.Models
@model List<TblSource>

@{
    string subject = null;
}

<div class="d-grid">
    @foreach (var s in Model)
    {
        if (subject != s.Subject)
        {
            <br />
            <h5 class="text-center">@s.Subject</h5>
            subject = s.Subject;
        }

        <a href="@s.Link" target="_blank" style="text-decoration: none;" class="btn btn-outline-dark" type="button">
            @s.Title
        </a>
    }
</div>
